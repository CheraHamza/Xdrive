<div class="location-bar">
  <% if (locals.location) { %>
  <% location.forEach((path, index) => { %>
  <h1><a href="<%= path.url %>"><%= path.name %></a></h1>
  <% if (index < location.length - 1) {%>
  <span class="material-symbols-outlined">keyboard_arrow_right</span>
  <% }}) } %>
</div>